/**
 * Import Statistics Frontend Panel
 * Entry point for the custom panel
 */

import './import-statistics-panel';

// The panel component is automatically registered via the @customElement decorator
// Panel registration with Home Assistant is handled in Python (__init__.py)
