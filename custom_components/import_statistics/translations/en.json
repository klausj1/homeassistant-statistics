{
    "services": {
        "import_from_file": {
            "description": "Imports long term statistic values (1h) from tsv/csv file",
            "fields": {
                "decimal": {
                    "description": "The decimal separator character: dot ('.') or comma (','). Default is dot ('.').",
                    "name": "decimal"
                },
                "delimiter": {
                    "description": "The delimiter between fields. Default is tab (\\t). Options: tab, semicolon, comma, or pipe",
                    "name": "delimiter"
                },
                "filename": {
                    "description": "Filename relative to config directory on the home assistant server (e.g., 'exported.tsv' or 'myData.json')",
                    "name": "filename"
                },
                "timezone_identifier": {
                    "description": "Timezone identifier (check pytz timezones or https://en.wikipedia.org/wiki/List_of_tz_database_time_zones). If not specified, uses Home Assistant's configured timezone",
                    "name": "timezone_identifier"
                },
                "datetime_format": {
                    "name": "datetime_format",
                    "description": "The format of the datetime strings in the imported file. Default is '%d.%m.%Y %H:%M'. d .. day, m .. month, y .. Year, H .. hour, M .. minute"
                },
                "unit_from_entity": {
                    "name": "unit_from_entity",
                    "description": "True: Take unit from existing entity (only valid for internal statistics, e.g. sensor.sun_solar_azimuth); False: Take unit from own column in imported data"
                }
            },
            "name": "Import statistics from file"
        },
        "export_statistics": {
            "name": "Export Statistics to file",
            "description": "Exports long term statistic values (1h) to a tsv/csv or a json file",
            "fields": {
                "filename": {
                    "name": "filename",
                    "description": "Filename relative to config directory on the home assistant server (e.g., 'exported.tsv' or 'myData.json')"
                },
                "entities": {
                    "name": "entities",
                    "description": "List of statistic IDs or entity IDs to export (supports both internal and external). Leave empty to export all statistics. Make sure to use a Yaml list with '- '"
                },
                "start_time": {
                    "name": "start_time",
                    "description": "The start time for statistics export in the format %Y-%m-%d %H:%M:%S (must be full hour, e.g. '2025-12-22 12:00:00'). If omitted, export starts from the earliest available statistic. Make sure to use quotes around the timestamp in Yaml."
                },
                "end_time": {
                    "name": "end_time",
                    "description": "The end time for statistics export in the format %Y-%m-%d %H:%M:%S (must be full hour, e.g. '2025-12-22 12:00:00'). If omitted, export ends at the most recent available statistic. Make sure to use quotes around the timestamp in Yaml."
                },
                "split_by": {
                    "name": "split_by",
                    "description": "Split output into multiple files by statistic type: 'none' (default, combined file), 'sensor' (only mean/min/max), 'counter' (only sum/state/delta), or 'both' (write both files). Output files will use suffixes _sensors and _counters before the extension."
                },
                "timezone_identifier": {
                    "name": "timezone_identifier",
                    "description": "Timezone identifier (check pytz timezones or https://en.wikipedia.org/wiki/List_of_tz_database_time_zones). If not specified, uses Home Assistant's configured timezone"
                },
                "delimiter": {
                    "name": "delimiter",
                    "description": "The delimiter between fields. Default is tab (\\t). Options: tab, semicolon, comma, or pipe"
                },
                "decimal": {
                    "name": "decimal",
                    "description": "The decimal separator character: dot ('.') or comma (','). Default is dot ('.')."
                },
                "datetime_format": {
                    "name": "datetime_format",
                    "description": "The format of the datetime strings in the exported file. Default is '%d.%m.%Y %H:%M'. d .. day, m .. month, y .. Year, H .. hour, M .. minute"
                }
            }
        }
    },
    "config": {
        "step": {
            "user": {
                "description": "Do you want to add the Import Statistics integration to Home Assistant?"
            }
        }
    }
}